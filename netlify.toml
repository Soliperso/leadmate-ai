[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: let react-router handle client-side routes so direct loads /
# refreshes of /pricing, /directory, etc. don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
